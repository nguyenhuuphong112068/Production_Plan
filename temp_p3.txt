1347:         $('.personnel-row .overlap-badge').remove();
1391:                         if (p1.$row.find('.overlap-badge').length === 0) {
1392:                             $(`<i class="fas fa-exclamation-triangle text-danger ml-1 overlap-badge" title="Trùng lịch với ca khác (${p2.startStr}-${p2.endStr} tại ${p2.room})" style="font-size:0.8rem;"></i>`).insertAfter(p1.$row.find('.op-icon'));
1396:                         if (p2.$row.find('.overlap-badge').length === 0) {
1397:                             $(`<i class="fas fa-exclamation-triangle text-danger ml-1 overlap-badge" title="Trùng lịch với ca khác (${p1.startStr}-${p1.endStr} tại ${p1.room})" style="font-size:0.8rem;"></i>`).insertAfter(p2.$row.find('.op-icon'));